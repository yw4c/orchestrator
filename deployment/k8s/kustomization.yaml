apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml

secretGenerator:
  - name: orchestrator
    files:
      - ../../app.dev.yaml

generatorOptions:
  disableNameSuffixHash: true